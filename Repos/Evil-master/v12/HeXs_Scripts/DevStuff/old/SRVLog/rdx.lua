

local function MakeSafe(str)
	return str:gsub("[\"*%?|\'<>:]", "_")
end


hostname..".txt" 

local dir_sub = dir:gsub( "[\"*%?|<>:]", "_" ) //Credit to Kogitsune 
local logs = {}
local num = 0


if !file.Exists(dir_sub) then file.Write(dir_sub,os.date())
	print("Dir Created") end function rbx.log(pl,text)
	for k,v in pairs(player
	
end

function rbx.log(pl,text) --local num local hostname
end






